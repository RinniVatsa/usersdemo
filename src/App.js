import React from 'react';
import FormData from './FormData'

function App() {
    return ( <
        div className = "App" >
        <
        header className = "App-header" >
        <
        h1 className = "text-3xl font-bold text-center mt-8" > Form Submission < /h1> < /
        header > <
        FormData / >
        <
        /div>
    );
}

export default App;